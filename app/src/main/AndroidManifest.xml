<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.kimp.mustep">

    <application
        android:name=".MuStepApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MuStep"
        tools:ignore="AllowBackup">

        <meta-data
            android:name="com.here.sdk.access_key_id"
            android:value="${HERE_MAPS_ACCESS_KEY_ID}" />
        <meta-data
            android:name="com.here.sdk.access_key_secret"
            android:value="${HERE_MAPS_ACCESS_KEY_SECRET}" />

        <activity android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.PreferencesActivity"
            android:exported="false" />
    </application>

</manifest>
